model TimeSlot {
  timeSlotId BigInt @id
  startTime  DateTime
  endTime    DateTime

  createdAt DateTime @default(now())
  createdBy String?
  updatedAt DateTime @updatedAt
  updatedBy String?
  active    Boolean  @default(true)
}
